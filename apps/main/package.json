{
  "name": "@aibos/main",
  "private": true,
  "version": "1.0.0",
  "description": "AI-BOS main application",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "rimraf dist && tsc -p tsconfig.build.json",
    "start": "node dist/index.js",
    "lint": "eslint .",
    "type-check": "tsc -p tsconfig.build.json --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@aibos/auth": "workspace:*",
    "@aibos/utils": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.18.8",
    "eslint": "9.36.0",
    "rimraf": "6.0.1",
    "tsx": "4.20.6",
    "typescript": "5.9.3"
  }
}